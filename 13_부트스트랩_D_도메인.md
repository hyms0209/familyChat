# 부트스트랩 단계 D — 가족채팅 도메인 스캐폴드 + Git

| 항목 | 값 |
|---|---|
| 단계 | D / D (마지막) |
| 예상 시간 | 5~10분 |
| 산출물 | 가족채팅 MENU/Provider/screens 빈 스캐폴드 + SpecKit + Git init + README + .env.example |
| 선행 조건 | 단계 C 완료 (poc-bank 인프라 이식됨) |
| 다음 단계 | P-5.1 (디자인 토큰 코드 변환, `11_프롬프트_코드변환.md` §P-5.1) |

> 마지막 단계. 가족채팅 도메인을 채우고 프로젝트를 첫 커밋 가능한 상태로 만든다.

---

## 사용 방법

```
@13_부트스트랩_D_도메인.md 의 [프롬프트] 섹션 그대로 실행해줘.
```

---

## [프롬프트]

```
가족채팅 모바일 앱 부트스트랩 — 단계 D (도메인 스캐폴드 + Git)

▌ 작업 위치
/Users/lmh/Documents/Claude/Projects/가족채팅/mobile/

▌ 단계 D 작업 항목

A. 가족채팅 MENU 채우기
   mobile/src/navigation/NavigationTypes.ts 의 MENU 객체에 다음 도메인을 모두 추가:
   - SPLASH: { INDEX: '/splash' }
   - AUTH: { SIGN_IN, SIGN_UP, VERIFY, TERMS, PROFILE, MINOR_CONSENT }
   - FAMILY: { CHOICE, NEW, INVITE, INVITE_JOIN, MANAGE }
   - MAIN: { INDEX, HOME, CHAT, MAP, CALENDAR, MORE }
   - CHAT: { ROOM, ATTACHMENT }
   - EVENT: { EDIT, DETAIL }
   - ACADEMY: { LIST, EDIT }
   - LOCATION: { SETTINGS }
   - NOTIFICATIONS: { INDEX }
   - SETTINGS: { INDEX }
   (정확한 path 는 03_기술설계서.md §3.3.2 참조)
   
   RootStackParamList 에 각 라우트의 파라미터 타입 정의 (대부분 undefined, EVENT 와 CHAT.ROOM 만 id 파라미터)
   TAB_SCREENS 에 MAIN.HOME, MAIN.CHAT, MAIN.MAP, MAIN.CALENDAR, MAIN.MORE 추가

B. FamilyProvider 신규 작성
   mobile/src/providers/FamilyProvider.tsx 생성:
   - poc-bank 의 ModeProvider.tsx 를 참조해 비슷한 구조로
   - 노출: { primaryFamilyId, members, role, isLoading, refresh }
   - useFamily() 훅 export
   - 일단 mock 데이터로 동작 (Firestore 연결은 P-5.5 에서)

C. AuthCheckProvider 를 Firebase Auth 로 적응
   mobile/src/providers/AuthCheckProvider.tsx 의 BNK API 호출 부분을:
   - Firebase Auth onAuthStateChanged 리스너로 교체
   - 일단 코드 골격만 (실제 Firebase init 은 P-5.5)
   - useAuth() 훅 export

D. 빈 화면 폴더 스캐폴드
   mobile/src/screens/ 아래 다음 폴더 생성, 각 폴더에 .gitkeep:
   - base/ (BaseScreen.tsx 는 poc-bank 에서 그대로 가져오기)
   - auth/ (S01~S07)
   - family/ (S08~S11)
   - main/{home, chat, map, calendar, more}/
   - chat/ (S14, S15 — main/chat 과 별도 풀스크린 라우트)
   - event/
   - academy/
   - location/
   - notifications/
   - settings/

E. 빈 컴포넌트 폴더 스캐폴드
   mobile/src/components/ 아래 다음 폴더 + index.ts (export {} 빈 파일):
   - button/, card/, chip/, avatar/, input/, header/
   - list/, divider/, empty/, modal/, sheet/, toast/, badge/
   (각 폴더 컴포넌트는 P-5.2 에서 채움)

F. 빈 훅 폴더
   mobile/src/hooks/ 아래:
   - api/ (.gitkeep)
   - auth/, deeplink/, dialog/, router/, backhandler/, screen/, util/
   - util/usePretendardFont.ts 는 단계 C 에서 이미 복사됨

G. Zustand 스토어 placeholder
   mobile/src/store/ 아래:
   - authStore.ts, familyStore.ts, locationStore.ts, themeStore.ts, notificationStore.ts, devToolsStore.ts
   - 각 파일은 빈 Zustand store 로 (실제 상태는 P-5.5)

H. OutLink 구현체 placeholder
   mobile/src/deeplink/outlink/:
   - MainOutLink.ts (가족채팅 메인 진입)
   - InviteOutLink.ts (가족 초대 — familyhub://invite/{token})
   각 파일에 OutLink 추상 클래스 상속 + 빈 구현 (실제 로직은 추후)

I. API 도메인 클라이언트 placeholder
   mobile/src/api/:
   - queryKeys.ts (가족채팅 도메인: family/, chat/, event/, academy/, location/, notifications/)
     03_기술설계서.md §3.6.3 의 queryKeys 예시 그대로
   - firebase/firestore.ts, auth.ts, messaging.ts, storage.ts (빈 클래스/함수)
   - axios/familyClient.ts (외부 API용 — 일단 빈)

J. RootNavigator 작성
   mobile/src/navigation/RootNavigator.tsx:
   - Stack Navigator 와 Bottom Tab Navigator 결합
   - SPLASH 가 초기 라우트
   - 각 화면은 일단 빈 placeholder 컴포넌트 (View + Text "S## 화면")
   - poc-bank/src/navigation/RootNavigator.tsx 를 참조

K. App.tsx 최종화
   - composeProviders 에 FamilyProvider 추가 (가장 외곽)
   - Firebase initializeFirebase() 호출 활성화 (단, .env 가 없으면 skip)

L. SpecKit 디렉토리 복사
   - poc-bank/.specify/ → mobile/.specify/
   - poc-bank/.claude/commands/ → mobile/.claude/commands/
   - poc-bank/.cursor/commands/ → mobile/.cursor/commands/
   - poc-bank/.cursor/rules/specify-rules.mdc → mobile/.cursor/rules/specify-rules.mdc
   - .specify/memory/constitution.md 는 가족채팅용으로 교체:
     /Users/lmh/Documents/Claude/Projects/가족채팅/12_Constitution.md 의 본문을 mobile/.specify/memory/constitution.md 로 복사

M. .env.example
   mobile/.env.example 생성:
   APP_VARIANT=development
   EXPO_PUBLIC_FIREBASE_API_KEY=
   EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=
   EXPO_PUBLIC_FIREBASE_PROJECT_ID=
   EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=
   EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
   EXPO_PUBLIC_FIREBASE_APP_ID=
   EXPO_PUBLIC_KAKAO_NATIVE_APP_KEY=
   EXPO_PUBLIC_SENTRY_DSN=

N. README 작성
   mobile/README.md:
   - 한 줄 개요 ("가족채팅 모바일 앱 — React Native + Expo + Firebase")
   - 출처: poc-bank 아키텍처 베이스 명시
   - 셋업: clone → cd mobile → npm install → cp .env.example .env.local → 채우기 → npm start
   - 폴더 구조 요약
   - 부모 폴더 문서 링크: ../00_INDEX.md, ../01_PRD.md, ../03_기술설계서.md, ../12_Constitution.md, ../11_프롬프트_코드변환.md
   - 다음 단계: "P-5.1 디자인 토큰 변환"

O. firestore.rules / storage.rules / firebase.json placeholder
   mobile/firestore.rules — 03_기술설계서.md §5.2 본문 그대로
   mobile/storage.rules — §5.3 본문 그대로
   mobile/firebase.json — emulators 설정 + rules 매핑

P. functions/ 빈 스캐폴드
   mobile/functions/package.json — 빈 placeholder
   mobile/functions/src/index.ts — 빈 export
   (실제 Cloud Functions 는 P-5.5 ~ P-5.6 에서)

Q. Maestro 빈 스캐폴드
   mobile/.maestro/README.md 만 (시나리오는 후속)

R. Git 초기화
   cd mobile && git init
   git add .
   git commit -m "chore: bootstrap from poc-bank architecture template"

S. 자체 검증
   - npx tsc --noEmit 에러 0건
   - npx prettier --check . (실패하면 prettier --write .)
   - 가능하면 npx expo start --no-dev --check (config 검증)

▌ 작업 후 출력
- 생성된 파일 트리 (depth 2~3)
- poc-bank → mobile 매핑 결과 요약 (단계 C + D 합쳐서)
- 첫 커밋 hash
- "부트스트랩 완료 — 이제 P-5.1 (디자인 토큰 변환) 으로 진행"

▌ 규칙
- 한 응답 안에 다 못 끝내면 "K까지 했어, L~S 는 '계속' 입력해" 로 알려주기
- 도메인 스캐폴드는 모두 빈 placeholder — 실제 로직은 P-5.x 에서 채움
```

---

## 성공 기준

- [ ] `mobile/src/navigation/NavigationTypes.ts` 의 MENU 가 가족채팅 도메인 10개로 채워짐
- [ ] `mobile/src/providers/FamilyProvider.tsx`, `AuthCheckProvider.tsx` 신규/적응 완료
- [ ] `mobile/src/screens/`, `components/`, `hooks/`, `store/`, `deeplink/outlink/` 빈 폴더 + .gitkeep
- [ ] `mobile/src/api/queryKeys.ts` 가족채팅 네임스페이스로 작성됨
- [ ] `mobile/src/navigation/RootNavigator.tsx` 작성됨 (placeholder 화면)
- [ ] `mobile/App.tsx` 에 FamilyProvider 포함된 composeProviders
- [ ] `mobile/.specify/memory/constitution.md` 가 12_Constitution.md 내용
- [ ] `mobile/.env.example`, `mobile/README.md`, `mobile/firestore.rules`, `mobile/storage.rules` 존재
- [ ] `mobile/functions/` 빈 스캐폴드
- [ ] `npx tsc --noEmit` 에러 0건
- [ ] Git 첫 커밋 완료

## 실패 시 트러블슈팅

| 증상 | 해결 |
|---|---|
| 한 응답 안에 다 못 끝냄 | 채팅창에 "L 부터 계속" 같은 부분 재시도 |
| RootNavigator 의 화면 import 가 빈 폴더라 깨짐 | 빈 화면 컴포넌트를 inline 으로 작성 |
| Firebase initializeFirebase() 가 .env 없어서 에러 | 함수 안에서 환경변수 체크 후 skip |
| Git init 권한 거부 | 사용자에게 권한 승인 요청 |

## 부트스트랩 완료 후

`mobile/` 폴더가 다음과 같은 상태가 되어야 합니다.

- Expo 앱 부팅 가능 (`npm start` → QR)
- 모든 화면이 placeholder 로 보임 (S01 시작 화면 → MAIN.HOME 으로 이동 시 빈 화면)
- TypeScript strict 통과
- Git 첫 커밋 완료

이 시점에서 다음을 시작할 수 있습니다:
- **P-5.1** (`11_프롬프트_코드변환.md`) — 디자인 토큰을 `mobile/src/shared/tokens/*.ts` 로 코드화
- **Pencil 디자인** — `07_프롬프트_디자인시스템.md` P-1.1 부터 단계별 디자인 작업
