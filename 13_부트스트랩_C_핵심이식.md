# 부트스트랩 단계 C — poc-bank 핵심 파일 이식

| 항목 | 값 |
|---|---|
| 단계 | C / D |
| 예상 시간 | 5~10분 |
| 산출물 | poc-bank 의 Tailwind 설정, Router, Provider, Pretendard 등이 mobile/ 에 복사·적응됨 |
| 선행 조건 | 단계 B 완료 (mobile/ + 의존성 셋업) |
| 다음 단계 | `13_부트스트랩_D_도메인.md` |

> 이 단계는 poc-bank 의 **검증된 인프라 코드**를 mobile/ 에 그대로 이식한다. **가족채팅 도메인 코드(MENU, FamilyProvider, screens)는 단계 D 에서**.

---

## 사용 방법

```
@13_부트스트랩_C_핵심이식.md 의 [프롬프트] 섹션 그대로 실행해줘.
```

---

## [프롬프트]

```
가족채팅 모바일 앱 부트스트랩 — 단계 C (poc-bank 핵심 파일 이식)

▌ 출처와 대상
- 출처: /Users/lmh/Documents/work/source/react/busanpoc/poc-bank
- 대상: /Users/lmh/Documents/Claude/Projects/가족채팅/mobile

▌ 이번 단계에서 이식할 파일 목록 (그대로 복사하되 일부는 가족채팅용으로 적응)

A. Tailwind 설정 (적응 필요 — 컬러만 가족채팅으로 치환)
   - poc-bank/tailwind.config.js
     → mobile/tailwind.config.js
     → 단, theme.extend.colors 의 primary, secondary 만 가족채팅 컬러로 교체:
       * primary: 코랄 스케일 (#FF7E6B 기준 50~900 자동 그라데이션)
       * secondary: 연한 블루 스케일 (#7EB6E8 기준)
       * success/warning/error/info 도 04_와이어프레임.md §1.2 표 색상으로
     → fontFamily, fontSize, spacing, borderRadius 는 그대로
   - poc-bank/tailwind.plugin.font-styles.js
     → mobile/tailwind.plugin.font-styles.js (그대로)

B. Babel/NativeWind 설정 (그대로)
   - poc-bank/babel.config.js
     → mobile/babel.config.js (그대로 — react-native-worklets/plugin + nativewind/babel + babel-preset-expo with jsxImportSource)
   - global.css (Tailwind 진입) 와 nativewind-env.d.ts 도 셋업

C. Pretendard 폰트 (그대로 복사)
   - poc-bank/assets/fonts/Pretendard-Regular.ttf
   - poc-bank/assets/fonts/Pretendard-Medium.ttf
   - poc-bank/assets/fonts/Pretendard-SemiBold.ttf
   - poc-bank/assets/fonts/Pretendard-Bold.ttf
   - poc-bank/assets/fonts/README.md
     → mobile/assets/fonts/ 로 모두 복사
   - poc-bank/src/hooks/util/usePretendardFont.ts
     → mobile/src/hooks/util/usePretendardFont.ts (그대로)

D. Navigation 인프라 — 시그니처와 로직 그대로, MENU 만 빈 placeholder
   - poc-bank/src/navigation/Router.ts
     → mobile/src/navigation/Router.ts (그대로 — 메서드 시그니처와 로직 변경 없음)
   - poc-bank/src/navigation/NavigationTypes.ts
     → mobile/src/navigation/NavigationTypes.ts
     → 단, MENU 객체 안의 도메인은 모두 비우고 placeholder 만 남김:
       export const MENU = {
           SPLASH: { INDEX: '/splash' },
           // 가족채팅 도메인은 단계 D 에서 채움
       } as const
       export type RootStackParamList = {
           [MENU.SPLASH.INDEX]: undefined
       }
       export const TAB_SCREENS: (keyof RootStackParamList)[] = []
       export interface ScreenResultProp<T = any, D extends object = object> { screenType: T; data: D }

E. Providers — composeProviders 와 인프라 Provider (그대로 복사)
   - poc-bank/src/providers/ComposeProviders.tsx → mobile/src/providers/ComposeProviders.tsx
   - poc-bank/src/providers/QueryProvider.tsx → mobile/src/providers/QueryProvider.tsx
   - poc-bank/src/providers/DialogProvider.tsx → mobile/src/providers/DialogProvider.tsx
   - poc-bank/src/providers/ToastProvider.tsx → mobile/src/providers/ToastProvider.tsx
   - poc-bank/src/providers/LoadingProvider.tsx → mobile/src/providers/LoadingProvider.tsx
   - poc-bank/src/providers/DevToolsProvider.tsx → mobile/src/providers/DevToolsProvider.tsx
   - poc-bank/src/providers/DeeplinkProvider.tsx → mobile/src/providers/DeeplinkProvider.tsx (구조 동일, 등록될 OutLink 만 단계 D 에서 채움)
   - poc-bank/src/providers/AuthCheckProvider.tsx → mobile/src/providers/AuthCheckProvider.tsx (BNK API 호출 부분은 주석 처리, 단계 D 에서 Firebase Auth 로 적응)
   - ModeProvider 는 가져오지 말 것 (단계 D 에서 FamilyProvider 로 신규 작성)

F. Deep Link 시스템 (그대로 복사 — outlink 구현체는 단계 D)
   - poc-bank/src/deeplink/DeepLinkManger.ts (또는 .Manager.ts) → mobile/src/deeplink/DeepLinkManager.ts (이름 정정)
   - poc-bank/src/deeplink/OutLink.ts → mobile/src/deeplink/OutLink.ts
   - poc-bank/src/deeplink/OutLinker.ts → mobile/src/deeplink/OutLinker.ts
   - outlink/ 하위 구체 클래스는 가져오지 말 것 (단계 D)

G. API 인프라 (구조와 인터셉터만, 도메인 클라이언트는 단계 D)
   - poc-bank/src/api/axios/config.ts → mobile/src/api/axios/config.ts (그대로)
   - poc-bank/src/api/logger.ts → mobile/src/api/logger.ts
   - bnkPocClient.ts 는 가져오지 말 것 (단계 D 에서 familyClient.ts 로)

H. Utility (그대로)
   - poc-bank/src/utils/Logger.ts → mobile/src/utils/Logger.ts
   - poc-bank/src/utils/loadingManager.ts → mobile/src/utils/loadingManager.ts

I. Constants
   - poc-bank/src/constants/theme.ts → mobile/src/constants/theme.ts (그대로 또는 컬러 토큰만 가족채팅으로)

J. App.tsx — poc-bank 패턴으로 재작성
   - poc-bank/App.tsx 를 베이스로 mobile/App.tsx 작성
   - composeProviders 순서는 poc-bank 와 동일하되 ModeProvider 만 빼기
     (FamilyProvider 는 단계 D 에서 추가)
   - usePretendardFont 호출 그대로
   - initializeFirebase() 호출은 주석 처리 (단계 D 에서 활성화)
   - NavigationContainer 는 placeholder 화면만 보여주는 RootNavigator 임시 사용
     → 단계 D 에서 가족채팅 RootNavigator 로 교체

▌ 자체 검증 (커밋 전)
- mobile/ 에서 npx tsc --noEmit 에러 0건
- npx prettier --check . 통과 (실패하면 prettier --write . 한 번)
- 폰트 파일 4개 + Tailwind 설정 + Provider 7개 + Router 등이 모두 존재하는지 ls 로 확인

▌ 작업 후 출력
- 이식한 파일 목록 (소스 → 대상)
- 적응한 부분 (어떤 파일에 어떤 부분이 가족채팅용으로 변경됐는지)
- 단계 D 에서 추가로 만들어야 할 도메인 파일 목록 (FamilyProvider, gajok MENU, OutLink 구현체 등)
- "단계 C 완료 — 다음 단계 D (도메인 스캐폴드) 진행 가능"

▌ 규칙
- BNK·블록체인·Avalanche·Merchant 등 도메인 종속 코드 절대 가져오지 말 것
- ModeProvider, screens/, services/bnkPoc/ 가져오지 말 것
- 한 응답 안에 끝내기 어려우면 "F까지 했고 G~J 는 다음 입력" 으로 알려주기 (사용자가 "계속" 입력)
```

---

## 성공 기준

- [ ] `mobile/tailwind.config.js`, `mobile/tailwind.plugin.font-styles.js` 존재
- [ ] `mobile/babel.config.js` 에 `nativewind/babel` + worklets 포함
- [ ] `mobile/assets/fonts/Pretendard-*.ttf` 4개 + README.md 존재
- [ ] `mobile/src/hooks/util/usePretendardFont.ts` 존재
- [ ] `mobile/src/navigation/Router.ts`, `NavigationTypes.ts` 존재 (MENU 비어있음)
- [ ] `mobile/src/providers/ComposeProviders.tsx`, `QueryProvider.tsx`, `DialogProvider.tsx`, `ToastProvider.tsx`, `LoadingProvider.tsx`, `DevToolsProvider.tsx`, `DeeplinkProvider.tsx`, `AuthCheckProvider.tsx` 존재
- [ ] `mobile/src/deeplink/DeepLinkManager.ts`, `OutLink.ts`, `OutLinker.ts` 존재 (outlink/ 하위는 비어있음)
- [ ] `mobile/src/api/axios/config.ts`, `mobile/src/api/logger.ts` 존재
- [ ] `mobile/src/utils/Logger.ts`, `mobile/src/utils/loadingManager.ts` 존재
- [ ] `mobile/App.tsx` 가 composeProviders 패턴으로 작성됨
- [ ] `npx tsc --noEmit` 에러 0건

## 실패 시 트러블슈팅

| 증상 | 원인 | 해결 |
|---|---|---|
| poc-bank 파일을 못 찾음 | Add Folder to Workspace 안 됨 | poc-bank 폴더를 워크스페이스에 추가 |
| 한 응답 안에 다 못 끝냄 | 파일 수가 많음 | 채팅창에 "G까지 했어. H/I/J 만 마저 해줘" 식 부분 재시도 |
| TS strict 에러 | poc-bank 의 일부 import 가 단계 D 파일에 의존 | 해당 import 만 임시 주석 처리 후 단계 D 에서 복구 |
| Pretendard 폰트가 너무 큼(빌드 오래) | TTF 파일 크기 | 정상 동작이니 진행 |
| AuthCheckProvider 가 BNK API 호출하는 줄에서 에러 | Firebase 미적응 | 해당 함수 본문을 빈 함수로 임시 교체 (단계 D 에서 적응) |

## 다음 단계 진입 조건

위 성공 기준 모두 OK + `npx tsc --noEmit` 통과 시 → `13_부트스트랩_D_도메인.md` 로 진행.
