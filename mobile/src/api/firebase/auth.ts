// Firebase Auth 서비스 계층. 단계 P-5.5 에서 실제 SDK 호출 구현.
export const authApi = {
    // signInWithEmail(email, password)
    // signInWithPhone(verificationId, code)
    // signInWithGoogle()
    // signInWithApple()
    // signInWithKakaoCustomToken(customToken)
    // refreshClaims()
    // signOut()
}
