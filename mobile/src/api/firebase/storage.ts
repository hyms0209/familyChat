// Firebase Storage 서비스 계층 (가족 사진/채팅 첨부).
// 5.3 Storage Rules — families/{familyId}/{path} 패턴.
export const storageApi = {
    // uploadFamilyImage(familyId, localUri): Promise<string>
    // uploadChatImage(familyId, chatId, localUri): Promise<string>
    // deleteByPath(path): Promise<void>
}
